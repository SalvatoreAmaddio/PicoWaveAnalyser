<Window x:Class="PicoWaveAnalyser.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PicoWaveAnalyser.Wpf"
        xmlns:converters="clr-namespace:PicoWaveAnalyser.Wpf.Converters"
        xmlns:vm="clr-namespace:PicoWaveAnalyser.Wpf.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Pico Waveform Analyser" 
        Height="600" Width="900"
        MinHeight="450" MinWidth="700">

    <Window.Resources>
        <converters:InvertBooleanConverter x:Key="InvertBooleanConverter"/>

        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        </Style>
        
        <Style TargetType="TextBox" x:Key="FolderPath" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Width" Value="500"/>
            <Setter Property="AllowDrop" Value="True"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Padding" Value="6"/>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Style="{StaticResource FolderPath}"
                     materialDesign:HintAssist.Hint="(You can drag and drop the folder here)"
                     materialDesign:HintAssist.HelperText="Waveform folder"
                     Text="{Binding FolderPath}" 
                     PreviewDragOver="Folder_DragOver"
                     Drop="Folder_Drop"/>

            <Button Grid.Column="1"
                    IsEnabled="{Binding IsAnalysing, Converter={StaticResource InvertBooleanConverter}}"
                    Command="{Binding BrowseCommand}"
                    Margin="10,0,0,0">

                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FolderOpen" Margin="0,0,6,0" Foreground="Gold"/>
                    <TextBlock Text="Browse"/>
                </StackPanel>
            </Button>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,15,0,15">
            <Button Command="{Binding AnalyseCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Play" Foreground="Green" Margin="0,0,6,0"/>
                    <TextBlock Text="Analyse" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CancelAnalysisCommand}" Margin="10,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Cancel" Margin="0,0,6,0" Foreground="Crimson"/>
                    <TextBlock Text="Cancel" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding ExportCommand}" Margin="10,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FileExport" Margin="0,0,6,0"/>
                    <TextBlock Text="Export CSV" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Filename" Binding="{Binding FileName}" Width="220"/>
                <DataGridTextColumn Header="Frequency (Hz)" Binding="{Binding FrequencyHz, StringFormat=F2}" Width="220"/>

                <DataGridTemplateColumn Header="Action" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Padding="10,3" Command="{Binding DataContext.OpenWaveformCommand,
                                                                     RelativeSource={RelativeSource AncestorType=Window}}"
                                                   CommandParameter="{Binding FilePath}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="OpenInNew" Margin="0,0,6,0"/>
                                    <TextBlock Text="Open"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <ProgressBar Grid.Row="3" Height="6" IsIndeterminate="{Binding IsAnalysing}" Foreground="#4CAF50" Background="Transparent" BorderThickness="0"/>
    </Grid>
</Window>